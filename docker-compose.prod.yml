version: '3.8'
services:
  web:
    restart: always
    build:
      context: ./
      dockerfile: ./web/Dockerfile
    ports:
      - "80:80"
    env_file:
      - ./web/.env
  bot:
    restart: always
    build:
      context: ./
      dockerfile: ./bot/Dockerfile
    env_file:
      - ./bot/.env